---
- name: Update apt cache
  become: yes
  apt:
    update_cache: yes


- name: Copy Nginx configuration
  template:
    src: template.portfolio.deployguru.com.j2
    dest: /etc/nginx/sites-enabled/template.deployguru.com
  notify:
    - restart-nginx